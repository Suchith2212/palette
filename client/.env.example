# Production only — your Railway API URL (no trailing slash)
# Example: https://palette-api-production.up.railway.app
# Leave empty in local dev (Vite proxies /api and /uploads to localhost:3000).
VITE_API_ORIGIN=
