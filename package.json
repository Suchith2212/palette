{
  "name": "palette-workspace",
  "version": "1.0.0",
  "description": "Palette - The Fine Arts Club of IIT Gandhinagar. A modern web application for art submissions, e-exhibitions, and event management.",
  "private": true,
  "author": "Palette Fine Arts Club, IIT Gandhinagar",
  "license": "MIT",
  "scripts": {
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client run build && npm --prefix server run build",
    "start": "npm --prefix server run start",
    "verify": "npm --prefix client run typecheck && npm --prefix client run build && npm --prefix server run typecheck && npm --prefix server run build"
  },
  "dependencies": {
    "nodemailer": "^7.0.12"
  },
  "devDependencies": {
    "@types/nodemailer": "^7.0.5"
  }
}
